# Install torch from the cu128 index. Blackwell cards (sm_120, e.g. the RTX 5090) need
# CUDA 12.8 as a minimum; this build also works on older hardware.
#
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch==2.11.0
#   pip install -r requirements.txt
#
# scripts/setup.sh does both steps.
torch==2.11.0
numpy>=2.0
matplotlib>=3.8
jupyter>=1.0
nbconvert>=7.0

# The probe cross-check in section 12 fits scikit-learn's LogisticRegression alongside the
# hand-written one, on both arms. The notebook runs without it and reports only the
# hand-written row, but the cross-check is worth having.
scikit-learn>=1.4

# Deliberately absent: scipy, faiss, networkx, torchvision.
#
# The build-from-zero constraint governs the method: the KNN graph, label propagation, the
# metrics, union-find, the SE-ResNet trunk and both augmentation pipelines are hand-written.
# Section 14 checks the augmentation operations against torchvision; install it separately
# to run those cells. Nothing in the pipeline depends on it.
#
# tests/check_imports.py enforces this mechanically.
